post:
  tags: [Attendance]
  summary: Add Holiday
  operationId: createHoliday
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../../components/holiday/request.yaml
  responses:
    "200":
      description: Holiday Details Register Successfully
      content:
        application/json:
          schema:
            $ref: ../../../components/holiday/response.yaml
    "400":
      description: Invalid Details
      content:
        application/json:
          schema:
            $ref: ../../../components/common/error-response.yaml
    "500":
      description: Internal Server Error 
      content:
        application/json:
          schema:
            $ref: ../../../components/common/error-response.yaml

