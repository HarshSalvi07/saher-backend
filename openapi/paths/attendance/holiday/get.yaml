get:
  tags: [Attendance]
  summary: Get Holiday
  operationId: getHoliday
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    "200":
      description: Holiday Details Retrive Successfully
      content:
        application/json:
          schema:
            $ref: ../../../components/holiday/response.yaml
    "400":
      description: Invalid Details
      content:
        application/json:
          schema:
            $ref: ../../../components/common/error-response.yaml
    "500":
      description: Internal Server Error 
      content:
        application/json:
          schema:
            $ref: ../../../components/common/error-response.yaml

