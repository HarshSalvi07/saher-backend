post:
  tags: [Auth]
  summary: User Logout
  operationId: logoutUser
  responses:
    "200":
      description: User Logout Successfully
      headers:
        Set-Cookie:
          description: remove cookies
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: ../../components/auth/logout-response.yaml 
    "404":
      description: No Token Found 
      content:
        application/json:
          schema:
            $ref: ../../components/common/error-response.yaml


